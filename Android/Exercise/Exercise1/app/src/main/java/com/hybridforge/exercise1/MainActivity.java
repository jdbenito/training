package com.hybridforge.exercise1;

import android.support.v7.app.ActionBarActivity;
import android.os.Bundle;
import android.view.Menu;
import android.view.MenuItem;


public class MainActivity extends ActionBarActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);

        // TASK 1:
        // 1. Get a handle on the TextView inside the layout
        // 2. Create another string in res/values/strings.xml
        // 3. Load the new string in the text box
        // 4. Run the app


        // TASK 2:
        // 1. Replace the text view from TASK 1 with 2 editable text fields
        // 2. Add an activity indicator to the layout and set its visibility to 'gone'
        // 3. Add a button
        // 4. Create a click listener for the button that will show the activity indicator
        // 5. Run the app


        // TASK 3:
        // 1. Create another activity, call it TableActivity
        // 2. In the click listener in TASK 2, add a line of code that will launch the 'TableActivity'
        // 3. Run the app
    }
}
